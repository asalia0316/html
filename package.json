{
  "name": "my-html-site",
  "version": "1.0.0",
  "description": "A simple HTML site with Sass support",
  "scripts": {
    "build": "sass styles.scss styles.css",
    "deploy": "npm run build"
  },
  "dependencies": {},
  "devDependencies": {
    "sass": "1.93.3"
  },
  "private": true
}